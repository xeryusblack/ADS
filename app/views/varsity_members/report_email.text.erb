Listing Varsity Members

        Varsity Track
        Debater Position
        Acquired QP
        Total Debt

      <% @varsity_members.each do |varsity_member| %>
         <%= varsity_member.vm_id %>
         <%= varsity_member.last_name %>
         <%= varsity_member.varsity_track %>
         <%= varsity_member.debater_position %>
         <%= number_with_precision(varsity_member.total_acquired_quota_points, precision: 2, delimiter: ',') %>
         <%= number_to_currency(varsity_member.total_debt, :unit => "Php") %>
      <% end %>
